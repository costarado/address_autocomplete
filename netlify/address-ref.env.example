# Copy these into Netlify Site settings → Environment variables.
# Keep ADDRESS_REF_ENABLED=false until schema + seed data are loaded.

ADDRESS_REF_ENABLED=false
ADDRESS_REF_APPLY_NAMES=false
ADDRESS_REF_DEBUG=false

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
