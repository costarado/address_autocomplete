# Copy these into Netlify Site settings → Environment variables.
# 1) Run supabase/lookup_zip_merge.sql in Supabase SQL Editor (uses existing zip_merge).
# 2) Set SUPABASE_SERVICE_ROLE_KEY (secret), keep ADDRESS_REF_ENABLED=false until verified.
# 3) Flip ADDRESS_REF_ENABLED=true.

ADDRESS_REF_ENABLED=false
ADDRESS_REF_APPLY_NAMES=false
ADDRESS_REF_DEBUG=false

SUPABASE_URL=https://lnjdhdllyyftposkjdqh.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
